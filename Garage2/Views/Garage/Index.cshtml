@model IEnumerable<Garage2.Models.Garage>

@{

}
@using Garage2.Models

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <h2>Parkerade Fordon</h2>
    
    <div class="form-horizontal">
        @using(Html.BeginForm())
        {
            @Html.DropDownList("Selecttype",
                 new SelectList(Enum.GetValues(typeof(Search))),
                 "Select Type",
                                new { @class = "form-control" })
            
                <input type="search" name="searchString" class="form-control" />
                <input type="submit" value="Search" class="btn btn-primary"  id="searchSubmit"/>
            
        }
    </div>
    
    @*<form method="get">
        <input type="search" name="searchString" />
        <input type="submit" value="Search" />
    </form>*@
    <p></p>
    <div class="table-responsive">
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.RegNr)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Color)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Type)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Parkerad)
                </th>
                @*<th>
                        @Html.DisplayNameFor(model => model.Brand)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.NumberOfWheels)
                    </th>*@


                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.RegNr)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Color)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Type)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Parkerad)
                    </td>
                    @*<td>
                            @Html.DisplayFor(modelItem => item.Brand)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.NumberOfWheels)
                        </td>*@


                    <td>
                        @*@Html.ActionLink("Edit", "Edit", new { id = item.Id })*@ |
                        @Html.ActionLink("Detaljvy", "Detaljvy", new { id = item.Id }) |
                        @Html.ActionLink("Hämta ut", "Hamta_ut", new { id = item.Id })
                    </td>
                </tr>
            }

        </table>
        
    </div>
    <a class="btn btn-primary" href="@Url.Action("Parkera", "garage")">Parkera &raquo;</a>
    

</body>
</html>
