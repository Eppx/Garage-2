@model IEnumerable<Garage2.Models.Garage>

@{

}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <h2>Parkerade Fordon</h2>
    <p>
            @Html.ActionLink("Parkera Fordon", "Create")
        </p>

    <form method="get">
        <input type="search" name="searchString" />
        <input type="submit" value="Search" />
    </form>

    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RegNr)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Color)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Parkerad)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.Brand)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumberOfWheels)
            </th>*@
            
            
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.RegNr)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Color)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Type)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Parkerad)
                </td>
                @*<td>
                    @Html.DisplayFor(modelItem => item.Brand)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NumberOfWheels)
                </td>*@
               
                
                <td>
                    @*@Html.ActionLink("Edit", "Edit", new { id = item.Id })*@ |
                    @Html.ActionLink("Detaljvy", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Hämta ut", "Delete", new { id = item.Id })
                </td>
            </tr>
        }

    </table>



</body>
</html>
